# 2026-06-11 开发日志

> 接 `2026-06-10-pipeline.md`(沙盒数据管道 A-D 已完成)。今天做剩下能独立推的:audit 升级 + A2。

## 今日完成

- [x] **audit-seo 升级:通用出站链接检查**。把"没泄漏 deepseek"升成 **访问链接(?ref=moxie)域名必须 == 产品展示域名 phUrl**(从页面自身提取,不需 DB)→ catch 任何"访问按钮指向错站",不止 deepseek。当前 142 页全过。
- [x] **A2:discover-domestic 迁 screen(trusted 模式)**。
  - 先试 `skipGate`(只跳规则闸)→ **实测 5 个种子 3 个被 aiClean 误拒**(无 og 触发"宁缺勿滥")→ 证明只跳闸不够。
  - 正解:**aiClean 加 `trusted` 模式**(`SYS_TRUSTED` 只归一、恒 keep,凭模型确知信息产出,价格红线保留);`callLLM` 改接受 sys 参数;抽 `clampNormalized` 复用。`screen({trusted})`= 跳闸 + aiClean trusted。
  - discover-domestic 改用 `screen(trusted)`,删掉自带第二份 enrich;**顺手修 clobber**(merge-duplicates→ignore-duplicates + slug 后缀循环,与 discover-tools 齐平)。
  - 验证:trusted 5/5 种子全 keep(讯飞星火/跃问/Llama/通义听悟/火山方舟);非 trusted 路径(discover-tools mock)无回归;pipeline.test 12/12;discover-domestic --dry-run 74 种子 dup-skip 0 失败。
  - 小瑕疵:Llama 的 domestic_available 被判"是"(应需代理)——模型误判,人工 QA 兜底;与旧 enrich 同病,非回归。
  - **至此两个发现源(PH 自动 / 国产 curated)统一走 screen**,A 阶段全完成。

- [x] **补自审挖出的两个缺口(上帝视角复审后)**:
  - ① **`clampNormalized` 抽到 `lib.mjs`(纯函数)+ 5 单测**(合法保留+截断 / 非法分类留空 / 价格·国内可用兜底枚举 / tags 截4每个≤12 / null 安全)→ 堵住 aiClean **生产路径(cron 真写)的归一钳制盲测**。坑:重构中途把 ai-clean 的 `gate` import 误删(它 self-test 在用),grep 抓回——印证"重构必查被删符号还有没有用"。
  - ② **audit-seo demo 检查通用化**:面包屑名 == h1 产品名、访问链接域名 == phUrl(不再只盯 deepseek 字面值)→ 模板换任何演示产品都能照出。
  - node:test 12→**17**;runtime mock 验 ai-clean import lib 正常、非 trusted 无回归。

## 当前可复用脚本/工具
clean-gate / ai-clean(含 trusted)/ screen(含 trusted)/ discover-tools / discover-domestic / reject / audit-published / audit-seo / rank(--explain)/ lib + pipeline.test。

## 待办 / 阻塞
- E 迁生产:阻塞,待用户给生产 Supabase + latemai.com 归属;迁前对账 3 处 schema 分歧(domain UNIQUE / cover_url / 枚举 CHECK)。
