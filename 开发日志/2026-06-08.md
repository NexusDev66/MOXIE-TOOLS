# 2026-06-08 开发日志

## 今日完成
- [x] 项目方向纠正对齐:同一个 Supabase 直接读写(无 API)、主链路全 AI 自动、数据要有价值、SEO 走预渲染。
- [x] 摸清现状:静态站 `moxie-supabase.js` 客户端直连同库,读 `moxie_products(published)`/`moxie_articles(published)`;上一版根因(API 错前提 / 没部署 / 数据是海外灰产)确认。
- [x] Phase 0.1 头部种子清单 `cli/seed-tools.json`(44 个国产+海外工具,10 分类全覆盖,domain 唯一、不撞库内 12 个、tagline≤30,已校验)。
- [x] Phase 0.2 入库脚本 `cli/seed-products.js`(ESM,幂等 upsert→published,带 dry-run,语法已过)。
- [x] `.env.local` 接线:公开值(URL/anon)已填,密钥留空待填。
- [x] 项目治理基建:`docs/`(需求规格/技术架构/设计规范/开发路线图/README 索引)+ 本开发日志 + 更新 `CLAUDE.md`。

- [x] 架构只读验证(anon key,无需 service key):线上库读路径通 —— published 产品 12 个(migration 种子在线)、文章 4、分类 10。证明"同库直连读"成立,缺的就是产品太少。

- [x] **决定改用独立沙盒 Supabase**(新项目 `kyiqgvxvbxktiygohuqh`,练手用,不碰生产库)。配置(.env.local / moxie-supabase.js / seed-products.js)全指向新库。
- [x] 新库建表:网页 SQL Editor 跑 `20260524183931_moxie_init.sql` 成功(6 表 + RLS + 12 种子)。
- [x] **Phase 0.3 完成**:填好 service_role secret → dry-run → 实插 44 → 复核 **56 个 published、10 分类全覆盖**(anon 读验证通过)。
- [x] git 仓库:本地 init + 首推 GitHub `NexusDev66/moxie-main-shiyan`(密钥未入库)。

- [x] **Phase 0.4 完成**:本地 `npx serve` 起站,`/moxie-preview` 肉眼确认 56 个工具显示正常(用户确认"打开了")。注:用户机器有代理(Clash 7890)会拦 localhost,关代理/绕过后正常。
- [x] Phase 0 配置改动已提交 + 推 GitHub(2b1cc3d)。

## 🎉 Phase 0 整体完成:数据闭环(库→读→静态站展示)真·跑通可见。

- [x] **Phase 1.1 完成**:`cli/prerender.js` 把 56 个 published 产品预渲染成 `tools/<slug>.html`(干净 URL /tools/<slug>);烤入 title/meta/canonical/OG/JSON-LD(SoftwareApplication)+ h1/desc 正文 + 写死 slug 供 hydrate;资源改根绝对。本地 serve 验证 `/tools/cursor` 源码爬虫可见内容。源模板未改,读数据用 anon。

- [x] **导航主页 `index.html`**(用户要 ai-bot.cn 表现力:分类侧栏 + 简洁工具卡 + 弱营销)。新建,不动原 moxie-preview;自包含 + 用品牌色 token + 读真数据(MoxieDB.categories/products),卡片链 /tools/<slug>;顶部搜索(客户端过滤)+ 侧栏跳转高亮。`/` 现在落地到它。待用户浏览器肉眼验 + 迭代视觉。

- [x] **改方案 B(用户确认)**:不另起页,把"AI 工具导航目录"(分类侧栏+简洁卡+搜索,td- 前缀自包含模块,复用 :root 变量)**插进 moxie-preview.html**(hero 之后),用户原板块(招牌/趋势/讨论/助手/文章/footer)全保留不动。`index.html` 改为跳转到 moxie-preview。先前还原了招牌块的"期数"(按日期自增)。
- [x] 验证:moxie-preview 含原板块 + 新目录;`/` 跳 moxie-preview;目录卡链 /tools/<slug>。

- [x] **再改(用户反馈页面太长/重复)**:删独立长目录,改为**中间排行榜列表上方加分类筛选 chip**(点分类→该类工具、全部→top12,用相同 .prow 样式)。
- [x] blog 区改两栏:近期文章 + **每日 AI 快讯热闻**(6 条示例,待接真源)。
- [x] 已提交 + 推送 GitHub(e90584e)。注:Phase 1.1 生成的 56 个 tools/*.html 也一并入库(暂当部署产物;Phase 1.4 再定 gitignore 与否)。

- [x] 快讯样式精修(红色实时点 + 脉冲徽标 + 密排),提交 aeee8bb。
- [x] **Phase 1.2 完成**:`cli/sitemap.js` → sitemap.xml(首页 + 56 个 /tools/,57 URL)+ robots.txt;首页 moxie-preview 补 description/canonical/og。本地服务 sitemap.xml/robots.txt 返回 200。提交 8147ab5。
  - 判断:首页大部分内容(招牌/文章/快讯/footer)本就静态烤在 HTML,只工具列表是客户端渲染;爬虫发现全站靠 sitemap + 已预渲染的 /tools/* 即可,故首页未强行整页预渲染。

## 待办 / 下一步
- [ ] 每日 AI 快讯:接真新闻源(RSS/抓取)——后面单独一小步。
- [x] **Phase 1.3 完成**:4 篇文章预渲染 articles/<slug>.html(title+内容+Article JSON-LD);sitemap 扩到 61 URL(首页+56产品+4文章)。注:种子文章无 body_html,正文暂空,待 Phase 4 生成。
- [x] **🎉 Phase 1.4 部署上线完成**:Vercel 连仓库部署成功,**公网 https://moxie-main-shiyan.vercel.app**。线上验证 /、/tools/cursor(含JSON-LD)、/articles/*、/sitemap.xml(61)全 200。修了线上 bug:vercel.json `cleanUrls:false`→`true`(否则 /tools/<slug> 干净 URL 404)。
- [x] refresh.yml(每日 rank+prerender+sitemap+提交→Vercel 重部署)已在仓库。
- [x] **🎉 Phase 2.4 完成**:用户加好 GH secret + 3 vars;手动触发 Daily Refresh,run **success**(rank/prerender/sitemap/提交 全步骤绿)。每日 cron 自动化闭环打通(每天北京9点:重算权重→重渲染→sitemap→推送→Vercel 重部署,全自动)。
- [x] **Phase 2 主体完成**:加 weight_score 列(SQL Editor)+ cli/rank.js(票数log+子墨+精选+完善度+时效,流量待key)算分写入 56 个;UI(moxie-supabase 默认排序 + 首页榜单 + 分类筛选)改按 weight_score 降序。Top:DeepSeek 94.5/Cursor 91.5。
- [ ] Phase 2.4:rank 接每日 cron(随部署)。
- [ ] 每日 AI 快讯接真源(后面单独一小步)。

- [x] **Phase 4 代码搭好(未真跑)**:`cli/gen-articles.js`(自包含 ESM,无 server-only 依赖)——取某分类 top 产品 → DeepSeek `/chat/completions`(json_object 模式)生成横评/选型/手册长文 → 写 `moxie_articles`。移植 T8 测过的 prompt 矩阵(3 模板 + 长尾词种子 + H2/H3 + FAQ)+ 解析(剥代码块/容错)+ 轻量消毒(去 script/on*/javascript:)+ 阅读时长估算。默认 `status=draft`(安全,审后发布),`--publish` 直接上线;幂等(同 slug 非 draft 不覆盖);带 `--dry-run`。语法 + guard(缺 DEEPSEEK_API_KEY 优雅退出)已验。
  - 跑法:`node --env-file=.env.local cli/gen-articles.js --category ai-coding --template compare [--limit 3] [--publish]`,之后 `prerender.js`+`sitemap.js` 即上线。
  - 待:用户填 `DEEPSEEK_API_KEY` 后真跑一篇验质量 → 满意再 `--publish` + 本地 commit 后推送。
- [x] **Phase 4 真跑 + 首篇上线**(用户填了 key):
  - 真跑 `ai-coding` 横评 → 文章 #5 `cursor-github-copilot-trae-compare`,质量达标(H2/H3+FAQ+长尾词,忠于资料)。
  - **价格红线**(用户指出"具体价格要以实测为准"):查实发现 LLM 编造了 `$20/$10/免费`(种子 `price_label` 只有"订阅"无数字)→ 改 SYSTEM_PROMPT 禁止编造金额,只定性 + 统一"具体价格以官网实测为准";重生成验证无金额(`54a2ff2`)。
  - 发布:直接 PATCH draft→published(不重生成,保留已审版本)→ prerender + sitemap → 推送 → Vercel(`46a4560`)。线上 200,正文/JSON-LD/canonical 均正确。
  - **修预渲染 bug**(验证时发现):① renderArticle 旧逻辑只把 body_html 插在 `#artBody` 开标签后、没删模板 fallback demo → 残留 `$2.5/M`/测试方法等;② 侧栏「本文涉及产品」「更多评测」写死 demo(DeepSeek/Claude/GPT-4o + 坏链),**所有文章页常驻、无 JS 重建**。修法:prerender 整段替换 `#artBody`(注入 h2 id)+ 整段重建 `<aside>`(目录按真 h2 / 涉及产品按 related_product_ids / 更多评测按其他文章)。一并修好全部 5 个文章页(`190736b`)。线上复验 demo 清零、涉及产品=Cursor/Copilot/Trae、价格红线在。

- [x] **首页版面迭代(用户多轮)**:① 近期文章/每日快讯从底部独立两栏 → 移入榜单**三栏**(文章左、榜单中、快讯右),后又按用户要求挪到**各栏最底**;删底部冗余 blog 区(去重 newsList)。② 快讯占位条**可点击**(暂 Bing 搜索跳转,待接真源)。③ 侧栏板块**卡片化**(白卡+1px 边+阴影)+ 侧栏改 **flex 列统一 18px gap**,板块间隔均匀。④ 榜单**前 5 名名次高亮**(前 3 橙加粗加大、4-5 橙浅一档)。
- [x] **两个缓存坑修复**(都导致"线上看不到变化"):① 改 CSS 必须 bump `moxie-preview.html` 里 `?v=` 版本号(已记 [[project_css-cache-bust]]);② `vercel.json` 的 `max-age=0` 只匹配 `*.html`,干净 URL(`/`、`/moxie-preview`、`/tools`、`/articles`)落到默认 CDN 长缓存 → 补上 `max-age=0,must-revalidate` 规则,HTML 改动即时可见。
- [x] 排查教训:`cleanUrls` 让 `.html` 网址 308 跳转,curl 验证要查干净 URL(`/moxie-preview`)而非 `.html`,否则 grep 到的是"Redirecting"桩。
- [x] **根 `/` SEO 修复**:删 `index.html` 空跳转桩 → `/` 经 rewrite 直接渲染完整首页(63KB,爬虫抓根即拿全内容,canonical 指向根 latemai.com),不再多一跳/空页。坑:`cleanUrls` 开启时 rewrite 目标不能是 `.html`(→404),改成 `/moxie-preview`(干净 URL)才生效。`/`、`/moxie-preview`、`/tools/*`、`/articles/*` 全 200。

- [x] **近期文章 / 快讯接真数据**:① 近期文章左栏改 JS 读真库最新 published 文章、链 `/articles/<slug>`(带写死兜底)。② **快讯接真源**(用户选 cron+建表方案):建 `moxie_news` 表(RLS 公开读)+ `cli/fetch-news.js`(多源 RSS、无依赖正则解析、去重、写库、留最新 60)+ `MoxieDB.news()` + 首页客户端读库渲染(链原文新标签、来源标签、相对时间、失败回退占位)+ 接入每日 cron(continue-on-error)。
  - 源选型:临时 workflow 在 **GitHub Actions(美国节点)** dry-run 实测可达性 —— 量子位 10/Solidot 20/36氪 20/机器之心 0。本机(国内+Clash)连不上这些源,故必须在 Actions 验。最终**只留量子位(纯 AI)**:Solidot/36氪 实测多为非 AI 科技/财经会污染"AI 快讯",机器之心取不到条目,均剔除。已删库里 Solidot 杂讯,剩 10 条量子位 AI。
  - 坑:Node 全局 fetch 不走 HTTP_PROXY,本机直连国内源失败;curl 走系统代理也连不上(code 000)→ 只能靠 Actions 验证。
- [x] **快讯加源**:Actions 实测候选源 → 加 **雷锋网**(20/天、新鲜、AI 聚焦),与量子位组成双源。再次排除:InfoQ(返回 2019 旧内容)、IT之家(60 条全消费电子非 AI)、机器之心 RSSHub(403)、品玩(405)。现首页 8 条全 AI。
- [x] **Phase 4 批量**:`gen-articles.js` 加 `--all` 模式(遍历分类,每个 ≥2 产品的分类生成一篇横评,复用 prompt/价格红线/去重)。真跑生成 **9 篇草稿**(llm/助手/写作/图像/视频/搜索/rag/agent/workflow;ai-coding 已有跳过)→ 抽检 2 篇(结构 5-6 H2+FAQ、1000-1350 字、0 编造金额、含官网红线)→ 全部发布 → prerender+sitemap → 上线。**文章总 14 篇**,sitemap 71 URL,新页均 200+Article JSON-LD。

- [x] **验收 + 修一个漏洞**:全站实测——8 条关键路由全 200、缓存头正确;56 产品/14 文章/32 快讯(雷锋13+量子7)/10 分类;权重榜正常(DeepSeek 94.4);14 篇文章 0 编造金额;每日 cron 自动跑(2026-06-09 09:01 定时触发)success。**发现并修复**:4 篇最初种子文章正文为空,prerender 旧逻辑"留模板默认"导致线上漏出假 demo($2.5/M 测试数据,每页 11 处)→ 改 prerender 空正文烤干净占位"正文整理中",线上复验 demo=0。**遗留**:这 4 篇仍是薄占位内容(待生成正文或下架,等用户定)。

## 阻塞 / 需对方配合
- `DEEPSEEK_API_KEY` 已填,Phase 4 首篇+批量已上线。
- 4 篇空种子文章(cursor-trae-windsurf / ai-content-saas-playbook / rag-vendor-selection-2026 / deepseek-v3-vs-claude)待用户定:生成正文 or 下架。

## 备注 / 决定
- 数据策略=头部成熟工具 + 新锐都要(C);SEO=预渲染/SSG(A)。
- 旧 docs(latemai-data-sync-design 等)标记为"错前提遗留",仅参考。
- 红线:密钥只在 .env.local,绝不进仓库/对话。
