@AGENTS.md

# Latemai 项目 · Claude 工作说明(必读)

> 一句话:基于 latemai 静态 UI,做"产品 AI 清洗 → 权重定时排序 → 同库直写 → SEO 预渲染"的全自动闭环。**同一个 Supabase,无内部 API。**

## 每次开工必做
1. 读 `docs/开发路线图.md` —— 看当前 Phase + 下一小步。
2. 读 `开发日志/` 最新一天 —— 看上次进度、待办、阻塞。
3. 写代码前读 `docs/设计规范.md` 红线。

## 标准文件路径
| 内容 | 路径 |
|---|---|
| 文档索引 | `docs/README.md` |
| 需求规格 | `docs/需求规格.md` |
| 技术架构 / 数据契约 | `docs/技术架构.md` |
| 设计规范 / 安全红线 | `docs/设计规范.md` |
| 开发路线图 / 执行步骤 | `docs/开发路线图.md` |
| 开发日志(每日) | `开发日志/YYYY-MM-DD.md` |

## 工作纪律(硬性)
- **小步 + 部署优先**:一次只做路线图一个小步,**部署 + 线上验证通过**才算完成、才下一步。本地跑过 ≠ 完成。(上一版教训:囤了一堆没上线的代码 = 0 产出)
- **每次收工写当天 `开发日志/YYYY-MM-DD.md`**(完成 / 待办 / 阻塞),新的一天新建文件。
- **密钥**只在 `.env.local`(已 gitignore)/ 部署端 env,绝不进代码/文档/commit/对话。
- **同一个 Supabase**(`sqvohgcwzhhsvkmyesvs`)直接读写;写用 service key(服务端),读用 anon+RLS(客户端)。不做内部 HTTP API。
- **真实性**:只声称真验证过的;没跑过不说跑过、不预勾验收。
- 旧 `docs/` 里 `latemai-data-sync-design.md` 等是"错前提"遗留,**仅参考,勿当权威**。
- push / 提交前自检无密钥、无 node_modules;未经同意不 push。
